sudo: required
services:
  - docker

before_install:
  - docker build -t zvaigzne/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true zvaigzne/docker-react npm run test -- --coverage